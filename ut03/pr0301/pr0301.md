# PR0301 Condicional if

## Ejercicio 1: Contar líneas de un archivo

1. Escribe un script que solicite el nombre de un archivo y luego imprima cuántas líneas tiene ese archivo. Verifica que el archivo exista antes de contar las líneas.

#!/bin/bash

echo "Introduce el nombre del archivo:"
read archivo

```
#!/bin/bash
if [ -e "$archivo" ];
then
    lineas=$(wc -l < "$archivo")
    echo "El archivo '$archivo' tiene $lineas líneas."
else
    echo "El archivo '$archivo' no existe."
fi
```

## Ejercicio 2: Verificación de archivo

2. Crea un script que compruebe si un archivo (cuya ruta pedirá al usuario por teclado) existe y si tiene permisos de lectura. Muestra un mensaje adecuado para cada caso.

```
#!/bin/bash

echo "Introduce la ruta del archivo:"
read archivo

if [ -e "$archivo" ]; then
    if [ -r "$archivo" ]; then
        echo "El archivo '$archivo' existe y tiene permisos de lectura."
    else
        echo "El archivo '$archivo' existe, pero NO tiene permisos de lectura."
    fi
else
    echo "El archivo '$archivo' no existe."
fi
```

## Ejercicio 3: Comparación de dos números

3. Realiza un script que solicite dos números al usuario y los compare, mostrando cuál es mayor, o si son iguales.

```
#!/bin/bash

echo "Introduce el primer número:"
read num1

echo "Introduce el segundo número:"
read num2

if [ $num1 -gt $num2 ]; then
    echo "El primer número ($num1) es mayor que el segundo ($num2)."
elif [ $num1 -lt $num2 ]; then
    echo "El segundo número ($num2) es mayor que el primero ($num1)."
else
    echo "Ambos números son iguales."
fi
```

## Ejercicio 4: Validación de contraseña

4. Escribe un script que solicite al usuario una contraseña y verifique si coincide con una contraseña predefinida (que estará almacenada en una variable de tu script). Si es correcta, muestra un mensaje de éxito, de lo contrario, indica que es incorrecta.

```
#!/bin/bash

contraseña_predefinida="miContrasena123"

echo "Introduce la contraseña:"
read -s contrasena_usuario

if [ "$contrasena_usuario" == "$contrasena_predefinida" ]; then
    echo "Contrasena correcta. Acceso concedido."
else
    echo "Contrasena incorrecta. Acceso denegado."
fi
```

## Ejercicio 5: Comprobación de directorio

5. Crea un script que compruebe si un directorio (que solicitará por teclado al usuario) existe y si tiene permisos de escritura. Si el directorio no existe, crea uno nuevo.

```
#!/bin/bash

echo "Introduce la ruta del directorio:"
read directorio

if [ -d "$directorio" ]; then
    if [ -w "$directorio" ]; then
        echo "El directorio '$directorio' existe y tiene permisos de escritura."
    else
        echo "El directorio '$directorio' existe, pero NO tiene permisos de escritura."
    fi
else
    echo "El directorio '$directorio' no existe. Creando el directorio..."
    mkdir "$directorio"
    if [ $? -eq 0 ]; then
        echo "El directorio '$directorio' ha sido creado con éxito."
    else
        echo "Error al crear el directorio '$directorio'."
    fi
fi
```

## Ejercicio 6: Verificar si el usuario es root

6. Haz un script que verifique si el script está siendo ejecutado por el usuario root, mostrando un mensaje diferente si no lo es.

```
#!/bin/bash

if [ "$(id -u)" -eq 0 ]; then
    echo "El script está siendo ejecutado por el usuario root."
else
    echo "Este script NO está siendo ejecutado por el usuario root."
fi
```

## Ejercicio 7: Calificación de un examen

7. Realiza un script que pida una nota numérica y determine si es “Aprobado” (5 o más) o “Suspenso” (menos de 5).

```
#!/bin/bash

echo "Introduce la nota numérica:"
read nota

if [ "$nota" -ge 5 ]; then
    echo "Aprobado"
else
    echo "Suspenso"
fi
```

## Ejercicio 8: Comprobación del espacio en disco

8. Crea un script que compruebe el espacio libre en disco. Si el espacio es inferior al 10%, muestra un mensaje de advertencia.

```
#!/bin/bash

espacio_libre=$(df / | awk 'NR==2 {print $5}' | sed 's/%//')

if [ "$espacio_libre" -ge 90 ]; then
    echo "Advertencia: El espacio libre en disco es inferior al 10%. ¡Limpieza recomendada!"
else
    echo "Espacio en disco suficiente."
fi
```

## Ejercicio 9: Menú de opciones

9. Escribe un script que muestre un menú con tres opciones. El usuario debe introducir una opción y el script debe ejecutar una acción diferente dependiendo de la opción seleccionada (es suficiente con que muestre un mensaje diferente según la opción escogida)

```
#!/bin/bash

echo "Seleccione una opción:"
echo "1) Opción 1"
echo "2) Opción 2"
echo "3) Opción 3"

read -p opcion

case $opcion in
    1)
        echo "Has seleccionado la Opción 1."
        ;;
    2)
        echo "Has seleccionado la Opción 2."
        ;;
    3)
        echo "Has seleccionado la Opción 3."
        ;;
    *)
        echo "Opción inválida. Por favor, selecciona una opción válida."
        ;;
esac
```

## Ejercicio 10: Evaluación de edad

10. Haz un script que solicite al usuario su edad y determine si es menor, adulto o mayor de edad, según un umbral predefinido (por ejemplo, menor de 18, entre 18 y 65, y mayor de 65).

```
#!/bin/bash

echo "Introduce tu edad:"
read edad

if [ "$edad" -lt 18 ]; then
    echo "Eres menor de edad."
elif [ "$edad" -ge 18 ] && [ "$edad" -le 65 ]; then
    echo "Eres adulto."
else
    echo "Eres mayor de edad."
fi
```